export interface Grid {
    index: number;
    gridWidth: number;
    gridHeight: number;
    list: Array<number>;
    moves: number;
}

export interface PuzzleData {
    index: number;
    list: Array<number>;
    moves: number;
}
